"""Metric utility."""

# khaleesi.ninja.
from khaleesi.core.metrics.metricInitializer import MetricInitializer as BaseMetricInitializer


class MetricInitializer(BaseMetricInitializer):
  """Collect info for initializing metrics."""

  def initializeMetrics(self) -> None :
    """Initialize the metrics."""
    super().initializeMetricsWithData(events = [])
