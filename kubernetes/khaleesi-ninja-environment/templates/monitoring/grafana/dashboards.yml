apiVersion: "v1"
kind: "ConfigMap"
metadata:
  name: "grafana-dashboards-khaleesi-{{ .Values.environment.name }}"
  namespace: "khaleesi-monitoring"
  labels:
    khaleesi-ninja: "environment"
    environment: "{{ .Values.environment.name }}"
    infrastructure: "grafana"
    grafana_dashboard: "1"
  annotations:
    khaleesi_folder: "khaleesi/{{ .Values.environment.name }}"
data:
  khaleesi-ninja.json: |
  {{- tpl ($.Files.Get "data/grafana-dashboards/khaleesi-ninja.json") $ | nindent 4 }}
